import { DocsPage } from '../components/DocsPage'

export function ReferencesPage() {
  return (
    <DocsPage
      folder="references"
      kicker="DOMAIN / REFERENCES"
      title="References"
      subtitle="external + internal reference material"
    />
  )
}
